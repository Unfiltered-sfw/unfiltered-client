const { createNewEntry } = require("../newEntry");

describe("createNewEntry", () => {
    
    test("that it is a function", () => {
        expect(typeof createNewEntry).toBe("function");
    })

    // test("it prevents page on reloading", )
})
