{
  "dependencies": {
    "handlebars": "^4.7.7"
  },
  
  "devDependencies": {
    "@types/jest": "^28.1.6",
    "jest": "^28.1.3"
  },
  "type": "module",
  "scripts": {
    "test": "jest --verbose --coverage --coverageReporters='text-summary'",
    "test:watch": "jest --watch"
  },
  "jest": {
    "verbose": true,
    "collectCoverage":true,
    "coverageReporters": [
      "html"
    ]
  }
}
